# 🔧 Инструкции по исправлению ошибок

## ✅ Исправленные проблемы

### 1. ✅ Hydration Error - ИСПРАВЛЕНО
**Проблема:** `typeof window !== 'undefined'` в `lib/firebase.ts` вызывал несоответствие между сервером и клиентом.

**Решение:** Удалил проверку `typeof window` и импорт Analytics из firebase.ts.

### 2. ✅ Бегущая строка теперь кликабельная - ИСПРАВЛЕНО
**Проблема:** Новости в Marquee не были кликабельными.

**Решение:** 
- Обновил `Marquee.tsx` для принятия объектов `{title, slug}` вместо строк
- Добавил `Link` компоненты для каждой новости
- Обновил `home/page.tsx` для передачи объектов с title и slug

### 3. ✅ Отображение изображений - ИСПРАВЛЕНО
**Проблема:** Next.js Image требует настройки для внешних доменов.

**Решение:** Обновил `next.config.ts`:
- Добавил поддержку всех HTTPS доменов
- Включил `unoptimized: true` для совместимости с ImgBB

### 4. ⚠️ Firebase Permissions - ТРЕБУЕТСЯ ДЕЙСТВИЕ

**Проблема:** `FirebaseError: Missing or insufficient permissions`

**Причина:** Правила безопасности Firestore не загружены в Firebase Console.

**Решение:** Загрузите правила безопасности (см. FIREBASE_SETUP.md)

#### Быстрое решение для тестирования:

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `yellow-news-65db7`
3. Firestore Database → Rules
4. Вставьте эти **временные** правила для тестирования:

\`\`\`javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
\`\`\`

5. Нажмите **Publish**
6. Обновите страницу сайта (F5)

⚠️ **ВАЖНО:** Это открывает базу для всех! Только для тестирования!

#### Правильное решение для продакшена:

Используйте правила из файла `firestore.rules`:

\`\`\`bash
firebase deploy --only firestore:rules
\`\`\`

Или скопируйте содержимое `firestore.rules` в Firebase Console → Firestore Database → Rules.

## 🔄 Что нужно сделать сейчас

1. **Обновите страницу** в браузере (F5 или Ctrl+R)
   - Hydration error должен исчезнуть
   - Marquee должна быть кликабельной
   - Изображения должны отображаться

2. **Загрузите правила Firebase** (см. выше)
   - Это исправит ошибку "Missing or insufficient permissions"
   - Комментарии начнут работать

3. **Протестируйте:**
   - Кликните на новость в бегущей строке
   - Попробуйте оставить комментарий (после загрузки правил)
   - Проверьте отображение изображений

## 📝 Дополнительные заметки

- Dev сервер автоматически перезагрузится после изменений
- Если видите ошибки TypeScript в IDE - это нормально, они исчезнут после перезапуска сервера
- Все изменения сохранены и готовы к использованию
