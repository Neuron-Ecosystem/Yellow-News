# Firebase Setup Instructions

## 🔥 Загрузка правил безопасности в Firebase

Чтобы исправить ошибку "Missing or insufficient permissions" и разрешить комментарии, нужно загрузить правила безопасности в Firebase Console.

### Вариант 1: Через Firebase Console (Рекомендуется)

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект **yellow-news-65db7**
3. В левом меню выберите **Firestore Database**
4. Перейдите на вкладку **Rules** (Правила)
5. Скопируйте содержимое файла `firestore.rules` из проекта
6. Вставьте в редактор правил
7. Нажмите **Publish** (Опубликовать)

### Вариант 2: Через Firebase CLI

```bash
# Установите Firebase CLI (если еще не установлен)
npm install -g firebase-tools

# Войдите в Firebase
firebase login

# Инициализируйте Firebase в проекте
firebase init firestore

# При инициализации:
# - Выберите существующий проект: yellow-news-65db7
# - Firestore Rules: firestore.rules
# - Firestore Indexes: firestore.indexes.json

# Загрузите правила
firebase deploy --only firestore:rules
```

### Временное решение для тестирования (НЕ для продакшена!)

Если хотите быстро протестировать, можете временно установить открытые правила:

1. Откройте Firebase Console → Firestore Database → Rules
2. Замените правила на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

3. Нажмите **Publish**

⚠️ **ВАЖНО**: Это открывает базу данных для всех! Используйте только для тестирования и обязательно замените на правильные правила из файла `firestore.rules` перед публикацией сайта.

### Проверка правил

После загрузки правил:
1. Обновите страницу сайта (F5)
2. Попробуйте оставить комментарий
3. Ошибка "Missing or insufficient permissions" должна исчезнуть

## 📝 Что делают правила безопасности

Правила в файле `firestore.rules`:
- ✅ Любой может читать новости и комментарии
- ✅ Только админы могут создавать/редактировать/удалять новости
- ✅ Авторизованные пользователи могут писать комментарии
- ✅ Пользователи могут редактировать только свои комментарии
